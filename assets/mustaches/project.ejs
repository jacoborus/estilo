name: '<%= it.name %>'
version: '<%= it.version %>'
license: '<%= it.license %>'
author: '<%= it.author %>'
url: '<%= it.url %>'
description: '<%= it.description %>'
colorschemes:
- name: '<%= it.name %>'
  background: 'dark'
  palette: '<%= it.name %>'
